# ARS FLOW 뷰어 — 테스트 가이드

## 0. 실행

```
1) 압축 해제 후 PyCharm 에서 ars_flow_test 폴더 열기
2) 터미널:  pip install -r requirements.txt
3) 실행:    python run.py     →  http://127.0.0.1:5000/topology
```

**실제 데이터로 테스트하려면** `scenario_cache/운영/` 안의 데모 파일을 지우고
실제 `*.dxml`(또는 `*.xml`) 전체를 복사한 뒤 새로고침하세요.
개발/QA 를 같이 보려면 `scenario_cache/개발·QA/` 폴더를 만들어 넣으면 상단 탭에 자동 노출됩니다.

---

## 1. 좌측 트리 (메뉴 구조)

| 확인 항목 | 기대 결과 |
|---|---|
| 첫 화면 | `ivrmain` 없이 **음성ARS Main · 인증 · 콜백 · 퇴직연금 · 신계약모니터링 · 방카** 가 바로 보임 |
| 음성ARS Main 펼치기 | 1~9번 대메뉴가 번호 배지와 함께 표시 |
| 대메뉴 계속 펼치기 | 중메뉴 → 소메뉴 → 액션까지 이어짐 (`·처리` = 액션) |
| 채널 필터 | **음성 / WebVoice / 통합** 전환. 통합에서 `WV`·`공통` 배지 확인 |
| 검색 | 시나리오명 일부 입력 시 필터링 |

**문제 시 확인**: 특정 대메뉴가 안 펼쳐지면 그 파일명을 알려주세요 (분기 패턴 차이 가능성).

---

## 2. 요약 (현업 제공용 구성도)

좌측에서 **대메뉴**(예: 4.부동산및 신용대출)를 클릭 → 상단 **요약** 탭

| 확인 항목 | 기대 결과 |
|---|---|
| 상단 | 기준일 · 시나리오 수 · 버전 해시 |
| 목차 | 액션 목록, 클릭 시 해당 위치로 스크롤 |
| 트리 | 메뉴 계층 유지, 각 노드가 **자기 몫만** 표기 (상위 처리 반복 없음) |
| 단계 칩 | `신청금액 입력 → 자격 검증 → … → 접수 완료` (내용 중심) |
| 세부 단계 | `1) 2) 3)` 번호로 분해, 조건은 한글 문장 |
| 보내는/받는 정보 | MCI 호출 단계에 한글 필드명 표시 |
| 이탈 경로 | 주황 박스로 "○○ 미충족 시 → 상담원 연결" |
| PDF | `PDF로 저장` → 액션 단위로 페이지 분리 |

**중점 확인**: 4번 외 다른 대메뉴(1·2·3·5·7·8번)에서도 같은 품질로 나오는지.

---

## 3. 상세 (개발자 모드)

액션 시나리오 선택 → 상단 **상세** 탭

| 확인 항목 | 기대 결과 |
|---|---|
| 구조 | **요약과 동일한 트리·STEP 구조** |
| 블록 카드 | 블록ID · 노드종류 · 조건식 원문(노란 박스) · 스크립트 전문(다크 코드블록) |
| 설정값 | `설정값 tMoney 대출신청금액` 형태로 변수+주석 |
| 입력 설정 | GetDigit 블록에 자릿수·종료키·멘트ID |
| 연결 | `← 이전 · true → 다음 · false → 예외` (블록ID 클릭 시 이동) |

---

## 4. 지도

| 선택 대상 | 기대 결과 |
|---|---|
| **메뉴** 노드 | 메뉴 계층 그래프 (DTMF 번호가 화살표 라벨, 직각 연결) |
| **액션** 시나리오 | 블록 흐름 그래프, 종료 노드(감사합니다·정상종료)는 **초록 굵은 테두리** |

**중점 확인**: 액션 지도에서 흐름이 **정상완료까지 이어지는지**.

---

## 5. 로그 연계 API (블록ID → 업무 위치)

브라우저 주소창에 그대로 붙여넣어 확인 가능합니다.

```
http://127.0.0.1:5000/api/topology/locate?env=운영&seq=00002813
```

여러 개 조회 (콤마 구분):
```
http://127.0.0.1:5000/api/topology/locate?env=운영&seq=00002813,00002889,00002815
```

**시나리오명까지 지정하면 정확** (권장):
```
http://127.0.0.1:5000/api/topology/locate?env=운영&seq=00002813&page=4111_신규대출일반.xml
```

기대 응답:
```json
{"results":[{"seq":"00002813","found":true,"matches":[
  {"page":"4111_신규대출일반.xml",
   "menu_path":["4-1-1. 신규대출","일반 대출신청"],
   "step_no":3, "step_title":"200만원 이하체크 외 12건 확인",
   "substep":"200만원 이하체크 조건을 확인합니다."}]}]}
```

> **주의**: 같은 `Sequence` 가 여러 시나리오에 중복 존재합니다.
> `page`(시나리오명) 없이 조회하면 후보가 여러 건 나옵니다.
> **실제 ARS 로그에 시나리오명이 함께 남는지 확인**해 주세요. 남는다면 그 값을 `page` 로 넘기면 정확히 1건으로 특정됩니다.

**테스트 방법**: 실제 로그에서 블록ID를 하나 골라 위 URL로 조회 →
그 위치가 로그의 실제 진행 지점과 맞는지 대조.

---

## 6. 배포 전후 비교 API

**(1) 배포 전 스냅샷 저장** — POST 라 브라우저로는 안 되고 터미널에서:
```
curl -X POST "http://127.0.0.1:5000/api/topology/snapshot?env=운영&tag=배포전"
```
Windows PowerShell:
```
Invoke-RestMethod -Method Post -Uri "http://127.0.0.1:5000/api/topology/snapshot?env=운영&tag=배포전"
```

**(2) 시나리오 교체** — `scenario_cache/운영/` 에 새 배포본 복사

**(3) 비교**
```
http://127.0.0.1:5000/api/topology/diff?env=운영&old=배포전
```

기대 응답:
```json
{"added_files":[], "removed_files":[],
 "changed":[{"file":"4111_신규대출일반.xml",
   "modified_blocks":[{"seq":"00002813","label":"200만원 이하체크",
     "location":{"menu_path":["4-1-1. 신규대출","일반 대출신청"],
                 "step_no":3,"step_title":"200만원 이하체크 외 12건 확인"}}],
   "added_blocks":[], "removed_blocks":[]}]}
```

**스냅샷 목록 확인**:
```
http://127.0.0.1:5000/api/topology/snapshot
```

**간단 검증 방법**: 스냅샷 저장 후, 시나리오 파일 하나를 텍스트 에디터로 열어
조건값(예: `&gt;= 200` → `&gt;= 500`)을 바꾸고 저장 → diff 호출 →
그 블록이 `modified_blocks` 에 잡히고 업무 위치가 맞게 나오는지 확인.
(확인 후 원복)

---

## 7. 진단 스크립트 (선택)

```
python scenario_diag.py scenario_cache/운영
```
정적/동적 이동 비율, 자동 연결 상한, 루트 후보를 출력합니다.
트리가 덜 이어질 때 원인 파악용입니다.

---

## 피드백 주실 때 유용한 정보

- 안 펼쳐지는 메뉴 → **시나리오 파일명**
- 이상한 한글 변환 → **원본 변수명 / 라벨**
- 엉뚱한 단계로 분류 → **항목명 + 맞는 단계**
- 로그 연계 → **로그 샘플 1줄** (블록ID·시나리오명이 어떻게 남는지)
