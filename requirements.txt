# Windows 로그 분석 시스템 요구사항 (OpenSSH 네이티브 버전)
# 로그 수집(grep/cat)은 Windows OpenSSH를 subprocess로 직접 호출합니다.
# paramiko 는 수집 경로가 아니라 'SSH 키 자동등록'(/servers/register-key)
# 한 곳에서만 쓰이며, 없으면 그 기능만 비활성화됩니다.

Flask==2.3.3
Flask-SocketIO==5.3.6
psutil==5.9.5
requests==2.31.0
Werkzeug==2.3.7
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7
blinker==1.6.2
python-socketio==5.8.0
python-engineio==4.7.1
bidict==0.22.1

# ── SSH 키 자동등록 (routes/servers.py: /servers/register-key) ────────
# 폐쇄망 반입: paramiko / bcrypt / pynacl / cffi / pycparser / cryptography whl
paramiko==3.4.0

# ── ARS(Windows) 네트워크 드라이브 접근용 (v4 인바운드) ───────────────
# win32wnet: WNetAddConnection2 로 ARS UNC 공유에 런타임 인증
# win32crypt: DPAPI 로 ars_auth.json 비밀번호 암호화/복호화
# 폐쇄망 반입: pywin32-312-cp313-cp313-win_amd64.whl (Python 3.13 / 64bit)
pywin32==312
